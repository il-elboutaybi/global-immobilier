{
  "name": "global-immobilier",
  "version": "1.0.0",
  "description": "Global Immobilier",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd client && npm run dev\" \"cd server && npm start\"",
    "build": "concurrently \"cd client && npm run build\" \"cd server && npm start\"",
    "dev": "concurrently \"cd client && npm start\" \"cd server && nodemon index.js\"",
    "deploy": "cd client && npm run build && cd server && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
